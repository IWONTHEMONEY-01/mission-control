[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
restartPolicyType = "on_failure"
numReplicas = 1

[[mounts]]
source = "mission-control-data"
dest = "/data"
